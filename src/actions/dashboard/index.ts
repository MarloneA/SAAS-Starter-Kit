'use server'

export const getUsers = async () => {
  try {
    const user = true
    if (user) {
      const clients = [{
        name: "droid"
      }]
      if (clients) {
        return clients
      }
    }
  } catch (error) {
    console.log(error)
  }
}
